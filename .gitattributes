# CSV публикуется как есть: перевод строки только LF на всех машинах.
# Иначе checkout на Windows/WSL подменил бы их на CRLF, и каждый прогон видел бы
# «файл изменился целиком» — история перестала бы показывать, что реально поменялось.
*.csv text eol=lf
*.md text eol=lf
